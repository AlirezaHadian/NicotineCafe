Drop your demo/attract-loop video here as: demo.mp4

Requirements:
- Any codec Windows Media Foundation can play (H.264 .mp4 is the safest bet).
- No specific resolution required — it's stretched UniformToFill to the
  screen, so use your screen's aspect ratio (usually 16:9) for the best fit.

This file is NOT included in the repo (no video shipped by default). If it's
missing, the demo simply never appears — nothing breaks.
